fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.0
PyJWT==2.10.1
httpx==0.28.1
python-multipart==0.0.20
reportlab==4.2.5

# Reading orders written in Devanagari or Kannada. A character-mapping library,
# not a model: pure Python, nothing downloaded at runtime, 0.2s to import.
indic-transliteration==2.3.82

# Preparing photographs for OCR: orientation, grayscale, contrast, and getting
# a phone-camera image under the free tier's 1 MB upload limit.
Pillow==12.3.0

# Event dispatch: Redis broker + Celery workers (see EVENT_DISPATCH)
celery==5.4.0
redis==5.2.1

# Dev tooling
pytest==8.3.4
ruff==0.16.7
